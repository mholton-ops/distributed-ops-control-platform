import type { Metadata } from "next";

export const metadata: Metadata = { title: "Sync Batches" };

export default function SyncBatchesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
